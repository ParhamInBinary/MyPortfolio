import { resumeStrings } from "../../../../assets/strings";

export const WorkExpComponent = () => {
    const { title, school, workExp, qualifications } = resumeStrings;
    return (
        <>
        {workExp.lunaCreatives.title}
        </>
    )
}